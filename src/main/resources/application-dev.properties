spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/animals_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:1234}

spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always
logging.level.org.springframework.jdbc.datasource.init=debug

jwt.secret=${JWT_SECRET:dGhpcy1pcy1hLXNlY3VyZS1hbmQtc3Ryb25nLXNlY3JldC1rZXktZm9yLWRldmVsb3BtZW50LXZhbHVlcy1vbmx5LXpvby1zeXN0ZW0=}
jwt.expiration=${JWT_ACCESS_EXPIRATION:900000}

# Admin en memoria. No se registra en la base de datos.
app.admin.enabled=${ADMIN_ENABLED:true}
app.admin.email=${ADMIN_EMAIL:admin@system-animals.ldb;ocal}
app.admin.password-hash=${ADMIN_PASSWORD_HASH:K9#mP2!xL$8vQzWb_7uR-eT4xE@pY1oN}
app.admin.roles=${ADMIN_ROLES:ROLE_ADMIN,ROLE_AUDITOR}

# Configuracion de correo real. Define estos valores como variables de entorno.
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:surielesrangel@gmail.com}
spring.mail.password=${MAIL_PASSWORD:sbed abnn wldz kwdh}
spring.mail.default-encoding=UTF-8
app.mail.from=${MAIL_FROM:${MAIL_USERNAME:no-reply@system-animals.local}}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=${MAIL_CONNECTION_TIMEOUT:5000}
spring.mail.properties.mail.smtp.timeout=${MAIL_TIMEOUT:5000}
spring.mail.properties.mail.smtp.writetimeout=${MAIL_WRITE_TIMEOUT:5000}


verification.code.rate-limit.max-requests=${VERIFICATION_CODE_MAX_REQUESTS:3}
verification.code.rate-limit.window-minutes=${VERIFICATION_CODE_WINDOW_MINUTES:15}
